import 'package:flutter/material.dart';

class ChatNotify extends StatelessWidget {
  const ChatNotify({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
