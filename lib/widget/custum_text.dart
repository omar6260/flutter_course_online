import 'package:flutter/material.dart';

class CustumText extends StatelessWidget {
  const CustumText({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
